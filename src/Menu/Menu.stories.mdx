import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Menu from './index';
import MenuItem from './MenuItem';
import FakeNextRouter from '../../.storybook/FakeNextRouter';

<Meta
	title="Interface/Menu"
	component={Menu}
/>

# Menu

```jsx
import Menu from 'ui/Menu';
<Menu items={[
	{ name: 'Trips', uri: '/trips' },
]} />
```

<Canvas>
	<Story name="Menu">
		<FakeNextRouter defaultPath="/trips">
			<Menu
				items={[
					{ name: 'Trips', uri: '/trips', icon: 'earth' },
					{ name: 'Templates', uri: '/templates', icon: 'grid' },
					{ name: 'Clients', uri: '/clients', icon: 'list' },
					{ name: 'Suppliers', uri: '/suppliers', icon: 'affiliate-details' },
				]}
			/>
		</FakeNextRouter>
	</Story>
</Canvas>

<ArgsTable of={Menu} />


#### `MenuItem`

<ArgsTable of={MenuItem} />

