import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Header from './index';
import FakeNextRouter from '../../.storybook/FakeNextRouter';
import Menu from '../Menu';
import ContextMenu, { ContextItemButton } from '../ContextMenu';
import Avatar from '../Avatar'; import Image from '../Image';

<Meta
	title="Layout/Header"
	component={Header}
/>

# Header

```jsx
import Header from 'ui/Header';
import Menu from 'ui/Menu';
import Avatar from 'ui/Avatar';

<Header
	logo={<div>[logo]</div>}
	menu={<Menu items={[]}></Menu>}
	contextMenu={<Avatar />}
/>
```

<Canvas>
	<Story name="Header">
		<FakeNextRouter defaultPath="/trips">
			<Header
				logo={<a href="#"><img src="https://tripmapper.ams3.digitaloceanspaces.com/1660831798130_example.svg" alt="Tam Org" style={{maxHeight:28,width:113}} /></a>}
				logoAction={{
					text: <>&larr; Back</>,
					href: '#',
				}}
				title="My Trip Name That's Really Long And Takes Up Way Too Much Space And Every Word Is Capitalized"
				menu={
					<Menu
						items={[
							{ name: 'Trips', uri: '/trips' },
							{ name: 'Templates', uri: '/templates' },
							{ name: 'Clients', uri: '/clients' },
							{ name: 'Suppliers', uri: '/suppliers' },
						]}
					/>
				}
				contextAction={{
					text: 'Edit',
					onClick: () => {},
				}}
				contextMenu={
					<ContextMenu menu={
						<>
							<ContextItemButton text="Hello" />
						</>
					}>
						<Avatar El="button" />
					</ContextMenu>
				}
			/>
			<Header
				pullUp
				logo={<a href="#"><img src="https://tripmapper.ams3.digitaloceanspaces.com/1660831798130_example.svg" alt="Tam Org" style={{maxHeight:28,width:113}} /></a>}
				menu={
					<Menu
						items={[
							{ name: 'Trips', uri: '/trips' },
							{ name: 'Templates', uri: '/templates' },
							{ name: 'Clients', uri: '/clients' },
							{ name: 'Suppliers', uri: '/suppliers' },
						]}
					/>
				}
				title="Another Trip Title"
				contextMenu={
					<ContextMenu menu={
						<>
							<ContextItemButton text="Hello" />
						</>
					}>
						<Avatar El="button" />
					</ContextMenu>
				}
			/>
			<Image src="https://source.unsplash.com/700x300?nature" width={700} height={300} />
		</FakeNextRouter>
	</Story>
</Canvas>

<ArgsTable of={Header} />

```ts
export interface HeaderActionProps {
	text: string;
	href?: string;
	onClick?: MouseEventHandler<HTMLButtonElement>;
}
```
