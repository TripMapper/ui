import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'; import TransactionItem
	from './index'; import { createClient, gql, Provider, useQuery } from 'urql'; import {
	IMAGE_FRAGMENT
} from '../Image';

<Meta
	title="Interface/Transaction Item"
	component={TransactionItem}
	decorators={[
		Story => (
			<Provider value={createClient({ url: 'https://dev.api.tripmapper.co/graphql', })}>
				<Story />
			</Provider>
		),
	]}
/>

# Transaction Item

<Canvas>
	<Story name="Transaction Item">
		{() => {
			const [{ data }] = useQuery({
				query: gql`{ asset (id: "6f0066f4-3071-11ec-b95d-93246b94e5e8") { id srcset (width: 40, height: 40) { ...Image } } }${IMAGE_FRAGMENT}`,
			});
			const shared = {
				name: 'My Transaction Item with a long name that has lots of words and is just unnecessary when you think about it, being concise should be every persons goal when im basically saying is shut up',
				status: 'BOOKED_PAID',
				budget: 123.40,
				tripBudget: 204.40,
				currency: { iso: 'USD' },
				tripCurrency: { iso: 'GBP' },
			};
			return (
				<>
					<TransactionItem
						{...shared}
						type="FOOD"
						image={data?.asset}
					/>
					<TransactionItem
						{...shared}
						type="ACCOMMODATION"
					/>
					<TransactionItem
						{...shared}
						type="ACTIVITY"
					/>
					<TransactionItem
						{...shared}
						type="FOOD"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
					/>
					<TransactionItem
						{...shared}
						type="OTHER"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_FLIGHT"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_DRIVE"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_TAXI"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_BOAT"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_PUBLIC_TRANSPORT"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_CYCLE"
					/>
					<TransactionItem
						{...shared}
						type="TRAVEL"
						subType="TRAVEL_WALK"
					/>
				</>
			);
		}}
	</Story>
</Canvas>

<ArgsTable of={TransactionItem} />
