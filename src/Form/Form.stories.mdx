import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'; import Form from '../Form'; import Input
	from '../Input'; import Button from '../Button'; import Label
	from '../Label';

<Meta
	title="Form/Form"
	component={Form}
/>

# Form

```jsx
import Form from 'ui/Form';

<Form onSubmit={/*...*/}>
	{/*...*/}
</Form>
```

<Canvas>
	<Story name="Form">
		<Form onSubmit={console.log}>
			<Label label="Numeric Text">
				<Input name="numericText" type="numeric" />
			</Label>
			<Button>Submit</Button>
		</Form>
	</Story>
</Canvas>

<ArgsTable of={Form} />
