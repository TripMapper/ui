/// <reference types="react" />
export default function Radio({ name, prefix, suffix, value, defaultChecked, }: {
    name: any;
    prefix?: string;
    suffix?: string;
    value?: string;
    defaultChecked?: boolean;
}): JSX.Element;
