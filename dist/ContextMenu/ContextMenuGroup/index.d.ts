import { ReactNode } from 'react';
interface ContextMenuGroupProps {
    El?: string | ReactNode;
    children?: any;
}
export default function ContextMenuGroup({ El, children, }: ContextMenuGroupProps): JSX.Element;
export {};
