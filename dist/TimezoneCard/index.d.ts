/// <reference types="react" />
export interface TimezoneCardProps {
    zone?: string;
    highlight?: boolean;
}
export default function TimezoneCard({ zone, highlight, }: TimezoneCardProps): JSX.Element;
